Bootstrap: docker
From: condaforge/miniforge3:latest

%files
    envs/lefse-r-env.yaml /opt/envs/lefse-r-env.yaml

%post
    conda env create -f /opt/envs/lefse-r-env.yaml -p /opt/conda-env
    conda clean -afy

%environment
    export PATH=/opt/conda-env/bin:$PATH

%runscript
    exec "$@"

%labels
    Source envs/lefse-r-env.yaml
