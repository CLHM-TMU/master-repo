Bootstrap: docker
From: condaforge/miniforge3:latest

%files
    envs/lefser-r-env.yaml /opt/envs/lefser-r-env.yaml

%post
    conda env create -f /opt/envs/lefser-r-env.yaml -p /opt/conda-env
    conda clean -afy

%environment
    export PATH=/opt/conda-env/bin:$PATH

%runscript
    exec "$@"

%labels
    Source envs/lefser-r-env.yaml
