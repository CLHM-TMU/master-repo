[Service]
# Make sshd near-immune to the OOM killer and guarantee it a memory floor,
# so a runaway Snakemake rule can starve everything else on the box without
# taking remote access down with it.
OOMScoreAdjust=-900
MemoryMin=256M
