Bootstrap: docker
From: condaforge/miniforge3:latest

%files
    envs/aldex2-r-env.yaml /opt/envs/aldex2-r-env.yaml

%post
    conda env create -f /opt/envs/aldex2-r-env.yaml -p /opt/conda-env
    conda clean -afy

%environment
    export PATH=/opt/conda-env/bin:$PATH

%runscript
    exec "$@"

%labels
    Source envs/aldex2-r-env.yaml
